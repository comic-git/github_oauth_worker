# Copy this file to Pulumi.bootstrap.yaml before initializing the bootstrap stack.
# The copied file is ignored because it holds deployment-specific configuration.
config:
  gcp:project: <gcp-project-id>
  github-oauth-worker-bootstrap:githubRepository: <github-owner>/<repository>
  github-oauth-worker-bootstrap:pulumiStateBucket: <bucket-name>
